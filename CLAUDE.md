# Claude Code Project Guidelines

## Branches

There are two branches: `main` and `custom`.

- `main` is a mirror of the upstream. We don't change it directly.
- We fork from `main` to `custom` for adding our own code.
